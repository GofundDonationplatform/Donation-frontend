import React from "react";

export default function Login() {
  return (
    <section className="container center" style={{paddingTop:40}}>
      <div style={{maxWidth:420, margin:"0 auto", color:"#fff"}}>
        <h2>Login</h2>
        <p className="muted">This is a placeholder login page. Connect to /api/auth for real auth.</p>
      </div>
    </section>
  );
}
